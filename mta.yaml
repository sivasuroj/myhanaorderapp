_schema-version: 3.3.0
ID: Myfirsthanaorderapp
description: A simple CAP project.
version: 1.0.0
modules:
- name: Myfirsthanaorderapp-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Myfirsthanaorderapp-auth
  - name: Myfirsthanaorderapp-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: Myfirsthanaorderapp-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Myfirsthanaorderapp-db
  parameters:
    buildpack: nodejs_buildpack
- name: Myfirsthanaorderapp
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: Myfirsthanaorderapp-auth
  - name: Myfirsthanaorderapp-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: Myfirsthanaorderapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Myfirsthanaorderapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - orders.zip
      name: orders
      target-path: resources/
    - artifacts:
      - custorder.zip
      name: custorder
      target-path: resources/
- name: orders
  type: html5
  path: app/orders
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: custorder
  type: html5
  path: app/custorder
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Myfirsthanaorderapp-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: Myfirsthanaorderapp-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: Myfirsthanaorderapp-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Myfirsthanaorderapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Myfirsthanaorderapp-html5-service
    service-plan: app-host
- name: Myfirsthanaorderapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Myfirsthanaorderapp-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
